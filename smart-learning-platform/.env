# ============================================================
#  生产环境变量（修改后生效）
#  启动: docker compose --env-file .env up -d
# ============================================================

# 应用端口
APP_PORT=8081

# MySQL
MYSQL_PASSWORD=smartlearning123
MYSQL_EXPOSE_PORT=3306

# Redis
REDIS_PASSWORD=redispass123
REDIS_EXPOSE_PORT=6379

# MinIO
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# JWT（生产环境务必更换！）
JWT_SECRET=ChangeMeToARandom64CharString!

# DeepSeek API（可选，不填则 AI 判卷功能不可用）
DEEPSEEK_API_KEY=
